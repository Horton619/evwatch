"""AutoTrader scraper (Mac-only, anti-bot protected).

Uses Playwright. Surfaces OEM-direct programs that other sources miss. Runs
from the Electron app via residential IP — not from GitHub Actions.
"""
